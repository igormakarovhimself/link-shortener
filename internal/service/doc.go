// Package service содержит бизнес-логику сервиса сокращения URL.
// Основной тип — ShortenerServiceImpl, который валидирует URL, генерирует короткие ссылки
// и асинхронно обрабатывает удаление через пул воркеров.
package service
